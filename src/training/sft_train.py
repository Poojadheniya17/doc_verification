"""SFT + QLoRA training entrypoint for extraction + tamper-detection. Config-driven; runs identically as a local CPU smoke test or a real Kaggle GPU run based on training_config.yaml's environment field.

Scaffolded in Phase 1. Implemented in Phase 4.
"""


def main(config_path: str) -> None:
    raise NotImplementedError("Implemented in Phase 4")
