# Core DL stack
torch
torchvision
# Pinned exact versions, not a floor: unpinned "transformers" now resolves to
# 5.14.1 on PyPI, a major-version jump past the 4.x line this project was
# built against (4.49 is the first release with Qwen2.5-VL support). Every
# Kaggle run through this project's SFT-training debugging saga used an
# unbounded "pip install -U", so exact versions could have silently drifted
# between runs, uncontrolled. Pinned here to match kernel_driver.py exactly.
transformers==4.57.6
qwen-vl-utils==0.0.14  # Qwen's own image/video preprocessing helper for the chat template
peft==0.19.1
trl
bitsandbytes==0.49.2
accelerate==1.14.0
diffusers

# Image / OCR
opencv-python
pillow
pytesseract
easyocr

# Retrieval / embeddings
sentence-transformers
faiss-cpu

# Decision layer / stats
scikit-learn
scipy
pandas
numpy

# Viz / demo
matplotlib
streamlit

# Experiment tracking
wandb

# Config / env
pyyaml
python-dotenv

# Kaggle CLI (train remotely; not needed for local CPU dev)
kaggle

# Dev / testing
pytest
